$env:PYTHONIOENCODING='utf-8'; python -c "import sqlite3; inn='5027246875'; conn=sqlite3.connect('tg_results.db'); conn.row_factory=sqlite3.Row; row=conn.execute('''SELECT inn, ogrn, company_name, okved, reg_date, company_status, director_name, director_inn, revenue_2024, income_2024, expenses_2024, authorized_capital, address, founders_json, length(raw_text) AS raw_text_len, source_query_id, created_at, updated_at FROM inn_results WHERE inn=?''',(inn,)).fetchone(); print(dict(row) if row else None); qid=row['source_query_id'] if row else None; q=conn.execute('SELECT id, query_text, created_at FROM source_queries WHERE id=?',(qid,)).fetchone() if qid else None; print('source_query:', dict(q) if q else None); conn.close()"